## Comandos úteis

### Criar um cluster

Para criar um cluster com o Kind basta:

```bash
kind create cluster
```

Passando um arquivo de configuração:
```bash
kind create cluster --config config.yaml 
```


### Deletar um cluster

Para deletar um cluster basta:
```bash
kind delete cluster --name nomedocluster
``` 
